# LEDGER — Repository Write / Delete / Lifecycle

> Decisions recorded symmetrically. Every **APPROVED** entry carries a non-empty falsification
> condition (the evidence that forces a revisit). Honor standing rejections; surface any approval
> whose falsification condition is later met.

| ID | Date | Decision | Type | Rationale | Evidence required to revisit (falsification) |
|----|------|----------|------|-----------|----------------------------------------------|
| **D1** | 2026-06-08 | **Delete dependency safety is owned by the concrete `deleteOne` (action tier), exactly once, in all engines.** `onQueryDelete` default becomes a plain `isOk`; the redundant prepare-phase `findChildrenNot` is removed from Mongo/SQL; memory gains the action-tier check. | APPROVED | All four principle lenses rejected the "`onQueryDelete` owns it" alternative: it makes referential integrity defeasible by any subclass that overrides the gate without calling `super`, relocating F2 into a future override. An engine-owned, unbypassable check is strictly more robust. | A concrete need for early dependency feedback in the Query.Delete prepare phase that an *advisory* (non-authoritative) pre-check cannot satisfy. (Advisory pre-check is allowed by I3 and does not reopen D1.) |
| **D2** | 2026-06-08 | **Canonical hook order = shared/`Upsert` hook outermost** (before: upsert→specific; after: specific→upsert), symmetric across create/update and identical across engines (I1). Reorder Mongo/SQL `updateOne` before-hooks and Mongo `updateFieldsById` query gates. | APPROVED | Unanimous across principle lenses. It ratifies the existing majority (all create paths, all after-hooks, all query gates, memory's update) and changes only the three genuine outliers — the smallest diff. The alternative institutionalizes a create/update asymmetry that every override author must special-case. | A downstream subclass with a documented, legitimate need for specific-before-shared ordering on update that cannot be satisfied by the symmetric order — i.e. a concrete case where `onBeforeUpdateAction` must observe the item *before* `onBeforeUpsertAction` runs. |
| **D3** | 2026-06-08 | **`onAfterOpen`/init runs exactly once before first use, surfaces failures, and is invoked uniformly across engines** (I4). Replace Mongo's fire-and-forget init with an awaitable `open()`/`initialize()` (minimum: a `CoroutineExceptionHandler` that marks the repository unready on failure). | APPROVED | A swallowed indexing failure can bring a collection up with no uniqueness enforcement while the app believes init succeeded; "same interface method means async-with-swallowed-errors in Mongo and never-runs elsewhere" is an untenable contract. | Evidence that synchronous/awaitable init causes an unacceptable startup-latency or deadlock regression in a real deployment that a supervised background-with-readiness-flag design cannot resolve. |
| **D4** | 2026-06-08 | **Add a first-class `allowApiCrud(apiItem: ApiItem.Action): SimpleState` gate** (default `isOk`) at the top of the `apiItemProcess` Action branch in all engines (I5). Subclasses override the gate, not the whole dispatcher. | APPROVED | The override-the-dispatcher pattern is correct but coarse, couples to dispatcher internals, and gates only one of several write surfaces (N1, N2). A default-OK hook is backward-compatible and gives origin-scoped policy a documented, portable home. | A requirement for write policy that varies *per operation/field* in a way a single boolean gate cannot express → escalate to D5 (`WriteOrigin`) or a richer policy object. |
| **D5** | 2026-06-08 | **Defer an explicit `WriteOrigin { Remote, Service }` marker on `ApiItem`.** Keep `call`-nullness as the trust/origin proxy for now, but *document* it (I6). | REJECTED (for now) | D4's `allowApiCrud` already gives origin-correct policy a home without a breaking change to the serialized `ApiItem` and every `Action` constructor. Adding `WriteOrigin` now would be breaking churn with no marginal benefit. | A security review that deems implicit `call==null`⇒trusted unacceptable, OR a service path that legitimately needs `call != null` while remaining trusted — either makes the proxy insufficient and reopens D5. |
| **D6** | 2026-06-08 | **Test strategy is layered:** a dependency-free cross-engine conformance suite (hosted in memorydb) for all portable invariants, PLUS one real-mongod write-failure test (Testcontainers preferred; flapdoodle fallback) for the F1 duplicate-key path. | APPROVED | A fix without a regression test is incomplete (Correctness First). memorydb alone cannot reproduce a real write failure, so it would leave the highest-risk path unverified; a real-mongod test is required for at least that case. | Docker/flapdoodle both unavailable in the target CI → fall back to memorydb-only conformance with a documented, explicit coverage gap for the real-write-failure path. |
| **D7** | 2026-06-08 | **Proportionate blueprinting:** this blueprint set (BRIEF/CONTRACT/LEDGER/PLAN) is the governance artifact. Narrow SAFE fixes proceed as direct construction; BREAKING contract changes cite CONTRACT.md + the relevant ledger entry. Project-wide cathedral adoption (CLAUDE.md config) is optional (PLAN G2). | APPROVED | This is a library bug-fix/contract-hardening cluster, not a greenfield feature; the premise requires the contract be written and test-pinned, not heavyweight ceremony disproportionate to blast radius. | The work grows beyond the listed scope (e.g. a new engine, or wire-surface changes) such that ad-hoc construction starts producing drift the lightweight artifacts don't catch. |
| **D8** | 2026-06-08 | **Close the SQL remote-write permission gap (N8).** `SqlRepository.apiItemProcess` now runs the per-action CRUD permission check (`call?.let { getCrudPermission(call, crudTask) }`) in its Action branch, matching its Query branch and Mongo's enforcement. The in-memory engine stays intentionally permission-free (samples/tests). | APPROVED | A SQL-backed repository behind the same RPC surface as Mongo previously skipped permission checks on remote Action writes — a correctness/security inconsistency (Correctness First). The fix is a no-op where no `rolePermissionProvider` is configured and only enforces where one exists, so it closes the gap without breaking unconfigured deployments. | A deployment that deliberately relies on SQL remote writes bypassing a configured permission provider — which would itself be a security defect, not a valid reopen. |

## Refuted / non-decisions (kept to prevent rejection amnesia)

- **F4 is not a bug.** The update-path `onValidate`-inside-`try` is safe today (non-local return).
  Do not re-raise it as an active defect; PLAN P1.2 hardens it structurally regardless (turns
  safe-by-coincidence into safe-by-structure), which is a separate, accepted motivation.
- **"All CRUD passes through `apiItemProcess`" is false** as a general statement (the stakeholder's
  objection). It is true only of the remote surface. The low-level/service tier is the layer
  *below* `apiItemProcess` and does not pass through it. Verified empirically (downstream SmokeM4)
  and by code trace.
- **After-hooks fire on *attempted* writes, not only successful ones** (review clarification of I2).
  Every engine fires `onAfter{Create|Update|Delete|Upsert}Action` exactly once for any write that
  reaches the driver call, passing `result = false` on failure; only the change-log is success-gated.
  Do **not** "fix" the after-hooks to fire only on success — that is intended (it lets hooks observe
  failures). The earlier KDoc wording that implied success-only was the documentation bug, now
  corrected in `IRepository` KDoc and CONTRACT.md I2.
